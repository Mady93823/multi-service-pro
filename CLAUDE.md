/# UrbanServe — Urban Company–style Home Services Platform

Client project. 3 roles (Customer / Provider / Admin), Laravel backend, live provider tracking, web-installer deployment.

## Source of truth

Read `docs/internal/00-Overview.md` first — it links the full doc set (architecture, modules, tech stack, DB schema, tracking spec, roadmap, conventions). The client-approved scope lives in `docs/client/Requirement-Analysis.md`.

## Hard constraints (client-mandated — never substitute)

- Backend: **Laravel 12** (PHP 8.3+), MySQL 8
- Frontend: **Inertia.js v2 + React 19 + TypeScript + shadcn/ui + Tailwind v4** — UI blocks researched via **shoogle.dev**
- Live tracking + realtime: **Laravel Reverb WebSockets + HTML5 Geolocation + Leaflet + OpenStreetMap** — no Node.js (client hosting ruled it out, ADR D11); single Laravel deployable
- Firebase: FCM push + optional phone OTP only (app must run with Firebase unconfigured)
- Payments: **Razorpay default** (UPI-first, INR, GST invoicing), Stripe secondary, pay-after-service enabled
- Must stay **easy installable** (web installer wizard) and **easy expandable** (domain events + settings flags + API-first)
- **White-label product rule (CodeCanyon build):** zero hardcoded branding/strings — settings + theme tokens + translation files (ADR D8–D10). Client-facing docs never mention resale.

## Working rules

- Follow build order in `docs/internal/06-Roadmap.md`; don't skip phase gates
- Code conventions, folder layout, and quality bar: `docs/internal/07-Conventions.md` (Actions pattern, thin controllers, Pest tests, Larastan L6, strict TS)
- Booking status changes only via `BookingStateMachine`; money columns are snapshots; ledgers append-only (`docs/internal/04-Database-Schema.md`)
- Scope/tech changes: update client doc + internal docs in the same commit; record decisions as ADR entries in `docs/internal/03-Tech-Stack.md`

## Current status

Phase 1 in flight — direction approved 2026-07-04. Decided: Razorpay; multi-city build, single-city India launch; India-first defaults; CodeCanyon product-ization; **Laravel-only realtime via Reverb (2026-07-06, D11 — client cannot host Node.js)**. 16 modules after Urban Company parity pass (M16 helpdesk; job-start OTP, cancellation fees, favorites/rebook, review photos added). Remaining client questions in client doc §10 (commission, payouts, OTP, brand, hosting, launch city — only Phase 4 blocks on them). Done: scaffold + Reverb smoke test, M01 auth/roles/layouts, M02 catalog (admin CRUD + storefront), M14 settings registry (branding/localization + admin UI), M15 installer base (`/install` wizard: requirements → DB/.env → migrate+seed → admin → lock), i18n skeleton (lang/en.json catalog + useTrans/`__()` everywhere + SetLocale middleware + catalog guard test — conventions in `docs/internal/07-Conventions.md` §i18n). Phase 1 gate (fresh-VPS wizard install) deferred by user — run before handover. Phase 2 started: M03 zones + addresses done (admin leaflet-draw polygon zones, customer address book w/ pin picker, Nominatim proxy cached+rate-limited, zone gate on catalog, PHP point-in-polygon ADR D12). M04 booking engine done (session-only cart w/ addons + guest merge, settings-driven slot picker, bookings + items + status-history tables, BookingStateMachine w/ 14 statuses + job-start OTP guard, address_snapshot JSON on bookings, cancellation fees flat/percent via settings, reschedule w/ provider release, favorites/rebook, problem photos on private disk, admin manual transitions, cash/pay-after-service only — gateways M08). M05 provider onboarding/panel done (provider_profiles/documents/categories/blackouts tables, `EnsureProviderApproved` middleware `provider.approved` gating panel w/ redirect to onboarding, profile form w/ bio/experience/categories/working-hours JSON/base-location pin/service-radius, KYC docs on private disk w/ replace-per-type + admin review queue, resubmission loop rejected→pending, online toggle + blackout CRUD, admin `/admin/providers` list+review incl. can't-approve-incomplete guard + suspend-forces-offline, `ProviderApprovalChanged` event for M11; blackout dispatch/slot enforcement deferred to M06). M06 dispatch & job assignment done (dispatch_offers table; app/Domain/Dispatch: DispatchStrategy interface + nearest/broadcast strategies, EligibleProviders finder — approved+online+category-or-ancestor+Haversine-within-radius+not-blackout+not-busy+not-already-offered, geo in PHP per D12; DispatchBooking/AcceptOffer/DeclineOffer actions, queued ExpireDispatchRound job guarded on expires_at so sync-queue installs degrade gracefully, BookingOffered/DispatchExhausted events; auto-dispatch on placement via DispatchPlacedBooking listener + dispatch.* settings mode/timeout/max_rounds/auto; accept = searching→assigned→accepted + expire siblings w/ row lock for broadcast race; provider Jobs screen w/ offer accept/decline + linear status buttons incl. job-start OTP; admin Run-dispatch button + offers list). **Phase 2 gate met:** booking placed → dispatched → accepted → completed with status buttons, all transitions in history.

Phase 3 started. M07 live tracking done (2026-07-09): tracking_sessions + tracking_points; app/Domain/Tracking (StartTrackingSession idempotent accepted→en_route, RecordTrackingPing validate→persist→broadcast w/ accuracy drop, EndTrackingSession en_route→arrived; LocationUpdated + BookingStatusBroadcast both ShouldBroadcastNow on private-tracking.booking.{id}; GeoPing DTO; tracking:prune scheduled daily). One `tracking.booking.{booking}` channel callback authorizes both the private and presence channel (returns member info). Provider journey screen (watchPosition throttled by tracking.ping_interval_seconds + min_move_meters, Wake Lock, permission-denied help, "customer is watching" presence); customer live map (react-leaflet + OSM, lerp-animated marker, trail, auto-fit yielding to user pan + Recenter, ETA, polling fallback on `bookings/{booking}/tracking/last` when Echo drops or pings go stale). **Reverb down never fails a ping** — point persisted first, BroadcastException logged. Tracking endpoints ride session auth on `web` routes, not Sanctum /api/v1 (ADR D13). M11 notifications done (2026-07-09): notifications + fcm_tokens tables; BookingStatusNotification + NewJobOfferNotification (ShouldQueue + afterCommit) over `database` + `broadcast` (private App.Models.User.{id}); FcmChannel exists but stays out of via() until Firebase creds are set (ADR D14, app must run with Firebase absent); bell dropdown + unread badge + useEchoNotification toast in both header shells + notifications index page. **Gotcha found & fixed: listeners in app/Listeners are auto-discovered — an extra Event::listen() double-fires them (M06's DispatchPlacedBooking had been dispatching twice). Register once; check `php artisan event:list`.** Tests pin BROADCAST_CONNECTION=null. **Remaining M07 gate: the two-physical-device acceptance checklist in docs/internal/05-Live-Tracking.md (real GPS over HTTPS) — not yet run.**

Phase 4 started. M08 payments + wallet done (2026-07-09, ADR D15): payments/wallets/wallet_transactions tables; app/Domain/Payments (PaymentGateway contract + Razorpay/Stripe gateways over the **raw HTTP client, no vendor SDKs**; GatewayManager; WalletService as sole wallet writer, locked apply() keeps `balance == credits − debits`; InitiateGatewayPayment reuses an open attempt, ConfirmPayment row-locked + idempotent, PayWithWallet, RefundBookingToWallet). **Cash starts `placed`; gateway/wallet start `pending_payment` and only reach `placed` once money settles — an unpaid online booking is never dispatched.** Razorpay callback + both webhooks HMAC-verified against the raw body; Stripe's return leg re-asks the API (`isSessionPaid`) — never trust a redirect. `webhooks/*` CSRF-exempt + throttled 60,1. `bookings:expire-unpaid` every 5 min; a payment landing on a terminal booking stays captured, logs a warning, refundable from admin. SettleCashOnCompletion (auto-discovered) settles cash on completion. Refunds v1 always credit the wallet (customer cancel = total − fee; admin cancel = full). **Gateway credentials live in settings, not .env, and secrets are write-only in the admin UI — the edit screen gets `*_set` booleans, never values (Inertia serializes every prop into the page HTML); blank = keep, `remove_*` = erase.** Screens: checkout method picker, customer pay page (Razorpay modal / Stripe redirect / wallet + expiry countdown), customer wallet ledger, admin settings Payments card, admin booking payments card + Refund dialog. Gotcha: the seeder completes a demo booking, so `SettleCashOnCompletion` means a cash payment row exists before any test runs — scope payment assertions to the booking, never `Payment::count()`.

M09 commission + payouts + GST invoice done (2026-07-10, ADR D16): `earnings` / `payout_requests` tables + `categories.commission_percent` nullable override; `app/Domain/Earnings` (CommissionResolver — per-item rate category→parent→global setting, charged on the **pre-tax** value, returns a blended rate; EarningsLedger read model; RecordBookingEarning / ReverseBookingEarning / RequestPayout / ProcessPayout). **`net = gross − commission − collected_amount`, and `collected_amount` is the customer's full total on a cash job — so a cash job's net is NEGATIVE (provider owes commission + the GST they took at the door). Never clamp it; payouts claim the negative rows so the debt offsets.** Debts skip `payouts.hold_days`; positive earnings serve it (`earnings:release`, daily). Ledger is append-only: a refund appends a `reversal` row negating the job row (`unique(booking_id, type)` backstop), so refunding a cash job credits the commission back. Listeners `RecordEarningOnCompletion` + `ReverseEarningOnRefund` (new `BookingRefunded` event) — auto-discovered, registered once. A payout claims the provider's **whole** released balance (`earnings.payout_request_id` back-reference), one open request at a time, rejection unclaims; the wallet is never touched — a payout is money leaving the system. GST invoice PDF via `barryvdh/laravel-dompdf` (`resources/views/invoices/booking.blade.php`, first Blade view beyond `app.blade.php`): figures from the booking's `tax_breakup` snapshot, number derived from the booking id so a reprint never renumbers, `BookingPolicy@invoice` = customer or admin (not the provider), `App\Support\Money` does Indian digit grouping without `ext-intl`. **Blade now counts for i18n — the catalog guard test and `scratchpad/reconcile_catalog.php` both scan `resources/views` for `__()`.** Gotcha: adding a required setting breaks `validSettingsPayload()` in `tests/Feature/Admin/SettingsManagementTest.php` — every settings PUT then 422s.

M10 reviews + ratings done (2026-07-10, ADR D17): `reviews` table (`booking_id` unique, `is_hidden` + `hidden_reason`); `app/Domain/Reviews` (SubmitReview — locked duplicate re-check + photo attach + provider notification; ModerateReview hide/unhide; ReviewChanged event). **`rating_avg`/`rating_count`/`jobs_completed` on `provider_profiles` are recomputed, never incremented** — listeners `SyncProviderRatingOnReviewChange` (over `visible()` reviews, so hiding a 1-star pulls it from the average) + `SyncProviderJobStatsOnCompletion` (fixes the dashboard counter stuck at 0 since M05), auto-discovered, registered once. Submission guarded in three layers: `BookingPolicy@review` (owner + completed), `SubmitReviewRequest` (rating 1–5, `reviews.max_photos`, duplicate, `reviews.enabled` kill-switch), action re-check under `lockForUpdate` w/ `unique(booking_id)` backstop. Review photos: `review_photos` medialibrary collection on private disk but the serve route is **guest-reachable** (storefront) — `ReviewPolicy@view` w/ nullable user, hidden → **404 not 403**; admin check inside the method because `before()` skips guests. Service page joins reviews through `booking_items` (multi-service booking reaches every service page it bought from) w/ avg + distribution bars + own `reviews_page` paginator. Screens: booking-show form/own-review card (owner sees hidden + reason), provider-dashboard recent reviews, admin `/admin/reviews` queue w/ visibility+star filters. **Gotcha: seeder order — ProviderSeeder now runs before BookingSeeder; the stat listeners no-op when the profile row doesn't exist yet.** Seeded demo: 3 reviews, provider at 4.67/3 ratings/3 jobs.

M12 coupons + banners + referrals done (2026-07-10, ADR D18): `coupons`/`coupon_usages`/`referrals`/`banners` tables + `users.referral_code` + FK on the dormant `bookings.coupon_id`. **Coupons:** `app/Domain/Coupons` — `CouponValidator` is the single eligibility+discount path (active/window/min_order/first_order_only/usage_limit/per_user_limit; flat vs percent w/ max_discount cap, clamped to the pre-tax base) used by the checkout apply endpoint (session `cart.coupon`, throttled 10,1 — codes guessable) AND re-run by `PlaceBooking` inside the placement transaction under `lockForUpdate` on the coupon row; discount enters `PriceQuote` **pre-tax** so M09's `CommissionResolver` needed no change. `coupon_usages` append-only (booking_id unique, created_at only, FK restrict): **spent at placement, cancel/refund never restores it; bookings dying `expired`/`failed_payment` stop counting via the join** — same two states define "first order". A stale session coupon is dropped w/ explanation on checkout reload; one that dies between apply and placement blocks the booking, clears itself, and the retry is clean. **Referrals:** referrer-only wallet credit through `WalletService` (referee's incentive = seeded WELCOME10 first-order coupon); registration w/ code (`?ref=` prefill, hidden when `referrals.enabled` off) creates a pending row via `RegisterReferral` (referee_id unique); `RewardReferrerOnFirstCompletion` (auto-discovered, once) fires on first completed booking → `RewardReferral` flips pending→rewarded under a row lock (double-fire safe), snapshots `reward_amount` **at reward time**, notifies via `ReferralRewardNotification`; `EnsureReferralCode` lazy-generates codes (never mass-assignable); reward 0 or program off = referral stays pending, not burned. **Banners:** medialibrary `image` collection on **public** disk (marketing, not user uploads) w/ hero/card webp conversions; `link_url` validated `url:http,https` (javascript: href = stored XSS); `scopeLive()` = active + window; storefront home renders rotating hero + scrolling strip, imageless banner falls back to gradient title card. Screens: checkout coupon box, wallet refer&earn card, register code field, home banners, admin `/admin/coupons` + `/admin/banners` CRUD (used coupon can't be deleted — deactivate; FK restrict backstop), settings Referrals card. Settings `referrals.enabled`/`referrals.reward_amount` (landmine: added to `validSettingsPayload()`). Seeded: WELCOME10 (10% first-order cap 200), FLAT50 (min 500, 2/user), 2 imageless banners via `MarketingSeeder`. 46 new tests (418 suite-wide). Next: M13 admin dashboard KPIs + reports + CSV export + admin support tools (roadmap Phase 5 item 2).

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
